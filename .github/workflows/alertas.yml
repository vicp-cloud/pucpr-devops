name: Alertas Discord
on:
  push:
    branches:
      - main
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar alerta para Discord
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{"content": "🚀 Novo commit enviado para o branch main!"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
