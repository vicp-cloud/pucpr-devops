name: Alertas Discord
on:
  push:
    branches:
      - Inicio
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar alerta para Discord
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{"content": "DEVOPS PUC PR - SOMATIVA 2 !"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
